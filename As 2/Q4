def count_occurrences(lst):
    freq = {}
    for num in lst:
        freq[num] = freq.get(num, 0) + 1
    return freq

print(count_occurrences([1, 2, 2, 3, 3, 3]))
print(count_occurrences([4, 4, 5, 6]))
print(count_occurrences([7, 8, 7, 9, 9]))